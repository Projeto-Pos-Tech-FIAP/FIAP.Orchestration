apiVersion: v1
kind: Service
metadata:
  name: user-api
  namespace: fiap-games
  labels:
    app: user-api
    tier: service
spec:
  selector:
    app: user-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

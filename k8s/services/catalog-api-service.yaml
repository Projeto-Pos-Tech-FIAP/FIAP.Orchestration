apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: fiap-games
  labels:
    app: catalog-api
    tier: service
spec:
  selector:
    app: catalog-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

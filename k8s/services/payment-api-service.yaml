apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: fiap-games
  labels:
    app: payment-api
    tier: service
spec:
  selector:
    app: payment-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

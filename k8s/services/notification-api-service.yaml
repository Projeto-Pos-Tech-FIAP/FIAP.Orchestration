apiVersion: v1
kind: Service
metadata:
  name: notification-api
  namespace: fiap-games
  labels:
    app: notification-api
    tier: service
spec:
  selector:
    app: notification-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: fiap-games
  labels:
    app: prometheus
    tier: monitoring
spec:
  selector:
    app: prometheus
  ports:
    - name: http
      protocol: TCP
      port: 9090
      targetPort: 9090
  type: ClusterIP

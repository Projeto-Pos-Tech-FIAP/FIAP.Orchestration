apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: fiap-games
  labels:
    app: kafka
    tier: infrastructure
spec:
  selector:
    app: kafka
  ports:
    - name: kafka
      protocol: TCP
      port: 9092
      targetPort: 9092
  type: ClusterIP

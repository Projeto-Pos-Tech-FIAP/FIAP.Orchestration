apiVersion: v1
kind: Service
metadata:
  name: sqlserver
  namespace: fiap-games
  labels:
    app: sqlserver
    tier: infrastructure
spec:
  selector:
    app: sqlserver
  ports:
    - name: mssql
      protocol: TCP
      port: 1433
      targetPort: 1433
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: fiap-games
  labels:
    app: redis
    tier: infrastructure
spec:
  selector:
    app: redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP

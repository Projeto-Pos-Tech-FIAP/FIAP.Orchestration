apiVersion: v1
kind: Secret
metadata:
  name: payment-api-secret
  namespace: fiap-games
  labels:
    app: payment-api
type: Opaque
# A PaymentAPI não tem banco relacional própria — o único segredo é a credencial do
# MongoDB, onde ela grava os logs dos eventos que consome e publica.
stringData:
  MongoDb__Username: "admin"
  MongoDb__Password: "PosTech@123"

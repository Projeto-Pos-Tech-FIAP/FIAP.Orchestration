apiVersion: v1
kind: Secret
metadata:
  name: payment-api-secret
  namespace: fiap-games
  labels:
    app: payment-api
type: Opaque
# PaymentAPI é stateless (sem SQL Server / MongoDB) — não tem segredos hoje.
# Mantido como recurso vazio para não exigir mudanças em kustomization.yaml/deployment.yaml
# caso um dia o serviço passe a precisar de credenciais.
stringData: {}
